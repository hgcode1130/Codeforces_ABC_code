#define _CRT_SECURE_NO_WARNINGS

#include <iostream>

#include<map>
#include<algorithm>
#include<cstring>
#include<vector>
#include<queue>
int main()
{
    freopen("in.txt", "r", stdin);
    freopen("std.txt", "w", stdout);
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d\n", a + b);
}
