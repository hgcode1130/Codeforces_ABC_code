#define _CRT_SECURE_NO_WARNINGS 1
#include<iostream>
using namespace std;
const int N = 2e5 + 10;

void solve() {
	string a, b;
	cin >> a >> b;
	swap(a[0], b[o]);
	cout << a << " " << b << "\n";

}
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	int t;
	cin >> t;
	while (t--) solve();

}